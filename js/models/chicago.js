define([
  'underscore',
  'backbone'
], function(_, Backbone) {
  var chicagoModel = Backbone.Model.extend({
    defaults: {
		
    },
    initialize: function(){
    }
    
  });
  return chicagoModel;

});