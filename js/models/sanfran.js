define([
  'underscore',
  'backbone'
], function(_, Backbone) {
  var sanfranModel = Backbone.Model.extend({
    defaults: {
		
    },
    initialize: function(){
    }
    
  });
  return sanfranModel;

});
