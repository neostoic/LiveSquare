define([
  'underscore',
  'backbone'
], function(_, Backbone) {
  var nycModel = Backbone.Model.extend({
    defaults: {
		
    },
    initialize: function(){
    }
    
  });
  return nycModel;

});
